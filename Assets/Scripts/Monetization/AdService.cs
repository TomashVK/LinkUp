public static class AdService
{
    // Stub until a real ad SDK is integrated — immediately grants the reward.
    public static void ShowRewardedAd(System.Action onComplete) => onComplete?.Invoke();
}
